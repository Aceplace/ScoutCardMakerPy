--core card maker

--fix exception handling (dont except Exception)
--add exception handling to script import 
--properly handle exceptions in power point export (simply skip the card)
--save a text file telling user which formations and defenses are nonexistent

--excel script import
--power point export

--configuration file on startup
--automatically load config file
--config file stores last saved libraries
--automatically load libraries on startup, if possible
--open/save dialogs open in previous loaded directory



ctrl-s saving

add apex placement rule
add more conditions
