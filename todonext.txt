--core card maker

--fix exception handling (dont except Exception)
--add exception handling to script import 
--properly handle exceptions in power point export (simply skip the card)
--save a text file telling user which formations and defenses are nonexistent

--excel script import
--power point export

--configuration file on startup
--automatically load config file
--config file stores last saved libraries
--automatically load libraries on startup, if possible
--open/save dialogs open in previous loaded directory

--add apex placement rule
--add more conditions (formation structure)
--    str/wk one receivers/two receivers/three receivers, nub/split, wing/pro/twin, bunch/indywing/indy

--change formation list box to not list "RT"

conditions -> 'or' conditions
strength type -> back strength

ask to save before closing
automatically save library when you save a defense or formation (maybe)
ctrl-s saving



refactors
change to string values in enum<-->Set up so formation works Direction.LT and Direction.RT
major refactor--> Change from imports to import as (makes it more obvious where definitions are coming from)
misc refactors



